# docs.python.org/3/library/functions.html#round

# Enter two strings and convert them into floats
x = float(input("What's x? "))
y = float(input("What's y? "))

# add them and show result
print(round(x + y,2))